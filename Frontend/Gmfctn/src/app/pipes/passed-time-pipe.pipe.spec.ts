import { PassedTimePipePipe } from './passed-time-pipe.pipe';

describe('PassedTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PassedTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
